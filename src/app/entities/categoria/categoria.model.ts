export class Categoria {
    titulo: string;
    url: string;
    categoria: string;

    constructor(titulo: string, url: string, categoria: string) {
        this.titulo = titulo;
        this.url = url;
        this.categoria = categoria;
    }
}
