export class Banner {
    titulo: string;
    url: string;
    descripcion: string;

    constructor(titulo: string, url: string, descripcion: string) {
        this.titulo = titulo;
        this.url = url;
        this.descripcion = descripcion;
    }
}
